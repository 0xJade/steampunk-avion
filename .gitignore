# build output
dist/
.astro/
.output/
.wrangler/

# dependencies
node_modules/

# secrets — never commit the real .ics token or PMS keys
.env
.env.*
!.env.example

# private per-property descriptions — kept local, not published to GitHub.
# The tracked _template.md is the exception so the folder/format survive.
src/data/descriptions/*.md
!src/data/descriptions/_template.md

# editor / os
.DS_Store
*.log
.vscode/
